<div class="row">
    <div class="col-xs-6 col-sm-3 col-md-2" ng-repeat="item in items">
        <div class="galleryImageWrapper" ng-click="open(items, $index);selected.item = item;">                 
            <img src="/images/uploads/thumbnails/thumbnail_{{item.name}}">
        </div>
     </div>
</div>
 